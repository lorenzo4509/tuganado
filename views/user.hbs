<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>User Profile</title>
  <style>
    body {
      background-color: #eafae2;
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
    }

    h1 {
      color: #448755;
      text-align: center;
      margin-bottom: 20px;
    }

    .user-info {
      background-color: #d7f6cd;
      border-radius: 5px;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      margin-bottom: 20px;
    }

    .user-info h2 {
      margin-bottom: 10px;
      color: #448755;
    }

    .liked-cows {
      background-color: #d7f6cd;
      border-radius: 5px;
      padding: 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    .liked-cows h2 {
      margin-bottom: 10px;
      color: #448755;
    }

    .cow {
      margin-bottom: 10px;
      color: #448755;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>User Profile</h1>
    <div class="user-info">
      <h2>User Information</h2>
      <p>Name: {{user.name}}</p>
      <p>Email: {{user.email}}</p>
    </div>
    <div class="liked-cows">
      <h2>Liked Cows</h2>
      {{#if user.likes.length}}
        {{#each user.likes}}
          <div class="cow">
            <p>Number: {{this.number}}</p>
            <p>Breed: {{this.breed}}</p>
            <p>Weight: {{this.weight}} kg</p>
            <p>Price: ${{this.price}}</p>
          </div>
        {{/each}}
      {{else}}
        <p>No liked cows yet.</p>
      {{/if}}
    </div>
  </div>
</body>
</html>
