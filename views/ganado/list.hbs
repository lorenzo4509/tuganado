<!DOCTYPE html>
<html>
  <head>
    <title>List of Cows</title>
    <style>
      body {
        background-color: #f8f8f8;
        font-family: Arial, sans-serif;
      }

      h1 {
        color: #336699;
        text-align: center;
      }

      table {
        width: 100%;
        border-collapse: collapse;
        margin-bottom: 20px;
      }

      th,
      td {
        padding: 10px;
        text-align: left;
      }

      th {
        background-color: #336699;
        color: #ffffff;
      }

      tr:nth-child(even) {
        background-color: #f2f2f2;
      }

      a {
        color: #336699;
        text-decoration: none;
      }

      button {
        background-color: #ffffff;
        border: none;
        padding: 0;
        cursor: pointer;
        transition: transform 0.3s;
      }

      .heart-icon {
        width: 20px;
        height: 20px;
        background-image: url('heart-icon.png'); /* Reemplaza 'heart-icon.png' con la ruta a tu icono de corazón */
        background-size: cover;
      }

      .heart-icon:hover {
        transform: scale(1.2);
      }
    </style>
  </head>
  <body>
    <h1>List of Cows</h1>
    <div class="add-button">
      <a href="/ganado/add">Add Cow</a>
    <table>
      <tr>
        <th>Number</th>
        <th>Breed</th>
        <th>Weight (kg)</th>
        <th>Price ($)</th>
        <th>Details</th>
        <th>Delete</th>
        <th>Like</th>
      </tr>
      {{#each cow}}
      <tr>
        <td>{{this.number}}</td>
        <td>{{this.breed}}</td>
        <td>{{this.weight}}</td>
        <td>{{this.price}}</td>
        <td><a href="/cow/{{this._id}}">Details</a></td>
        <td>
          <form onsubmit="fetch('http://localhost:3000/cow/{{this._id}}', { method: 'DELETE'})">
            <input type="hidden" name="_method" value="DELETE">
            <button type="submit">Delete</button>
          </form>
        </td>
        <td>
          <form action="/cow/like/{{this._id}}" method="POST">
            <button type="submit" class="heart-icon"></button>
          </form>
        </td>
      </tr>
      {{/each}}
    </table>
  </body>
</html>
